# 入库单/出库单 四态改造 + 反生效 + 退库单 技术方案

> 模块：`warehouseManagement/inboundManagement/inboundOrder`、`outboundManagement/outboundOrder`、退库单（新增）
> 后端：mxktech-ruoyi-warehouse（WarehouseInboundOrder / WarehouseOutStoreOrder / WarehouseReturnOrder 新增）
> 日期：2026-08-28

## 1. 目标与范围

1. 入库单、出库单统一为四态状态机：**草稿 → 已生效 → 已完成**，**已作废**为旁路终态。
2. 新增**反生效**（弃审）能力：生效后未执行可返回草稿修改，与 ERP 审核/弃审习惯一致。
3. 新增**退库单**：已完成的单据通过独立退库单反向冲销（红字 ERP），原单状态不变。
4. 全部操作权限通过若依**按钮级角色权限**控制，不写死。
5. 退库单无审批环节（草稿→生效→完成）。

## 2. 现状分析

### 2.1 入库单现状（问题清单）

| 项 | 现状 | 问题 |
|---|---|---|
| 状态 | 0未入库 / 1已入库 / 2已作废（三态） | 无"生效"环节；"1"同时承载"生效+入库" |
| 入库 | PUT updateById status=1：校验→占用待入库(is_store 0→1)→写库存→MRP补锁→推ERP(创建+审核)，@Transactional 原子 | 逻辑正确，但职责耦合在更新接口 |
| 作废 | PUT status=2（有 externalCode 则 ERP 弃审+删除）+ 前端 DELETE 物理删明细 | **物理删明细破坏审计** |
| 状态校验 | 后端编辑/作废分支不校验当前状态 | **绕过前端可非法操作** |
| 权限 | @PreAuthorize 全部注释 | 无按钮权限控制 |

### 2.2 出库单现状（与入库单差异）

| 项 | 现状 |
|---|---|
| 状态 | status：0草稿 / 1生效 / 2作废；outStoreStatus：0待出库 / 1已出库 / 2已完成（双维度） |
| 生效 | operation.vue 已提供"生效"按钮（status 0→1，提示"生效后不允许编辑"） |
| 出库 | PUT /confirm/{id}：仅 status=1 且 outStoreStatus=0；行锁校验可用量→扣库存→MRP锁定冲减→写出库流水→回写配货计划→发布线边仓事件→推ERP(销售出库)，@Transactional 原子 |
| 作废 | PUT /void/{id}：仅 status=1；ERP 弃审+删除；**若已出库自动回滚库存+配货计划+MRP恢复**（粗糙的冲销实现） |
| 编辑/删除 | 前端仅 status=0 显示 |
| 出库类型 | 字典含 2生产退料出库 / 3采购退货出库（手工退库入口，非关联原单） |

### 2.3 结论

出库单已有"草稿→生效"雏形；入库单完全缺失。统一改造两单，新增反生效与退库单。

## 3. 统一状态机设计

### 3.1 状态定义（两单 + 退库单统一）

| 值 | 名称 | 语义 | 可编辑 | 终态 |
|---|---|---|---|---|
| 0 | 草稿 | 制单中 | ✅ | ❌ |
| 1 | 已生效 | 锁定，未执行 | ❌ | ❌ |
| 2 | 已完成 | 过账完成（入库/出库/退库执行） | ❌ | ✅ |
| 9 | 已作废 | 放弃单据（旁路） | ❌ | ✅ |

### 3.2 操作与状态流转

```
                生效                执行(过账)
     草稿(0) ───────────► 已生效(1) ──────────► 已完成(2)
        ▲                    │                    │
        │     反生效(弃审)    │ 作废               │ 退库单(独立单据,原单不变)
        └────────────────────┘                    ▼
         (仅未执行可反生效)    ▼               退库单 草稿→生效→完成
                          已作废(9)
```

### 3.3 操作 × 状态矩阵（统一）

| 操作 \ 状态 | 草稿(0) | 已生效(1) | 已完成(2) | 已作废(9) |
|---|---|---|---|---|
| 保存草稿 | ✅ | ❌ | ❌ | ❌ |
| 编辑 | ✅ | ❌ | ❌ | ❌ |
| 删除 | ✅ | ❌ | ❌ | ❌ |
| 生效 | ✅ | ❌ | ❌ | ❌ |
| 反生效 | ❌ | ✅（未执行） | ❌ | ❌ |
| 执行（入库/出库） | ❌ | ✅ | ❌ | ❌ |
| 作废 | ✅ | ✅（未执行） | ❌ | ❌ |
| 退库（生成退库单） | ❌ | ❌ | ✅ | ❌ |
| 详情 / 打印 | ✅ | ✅ | ✅ | ✅ |

### 3.4 操作规则与后端状态校验（必做）

所有操作在 Service 层先查 DB 状态校验，**禁止依赖前端按钮隐藏**：

| 操作 | 接口 | 前置状态 | 规则要点 |
|---|---|---|---|
| 保存/编辑 | POST / PUT 主接口 | status=0 | 明细非空、批次占用校验（入库）；编辑=明细全量覆盖 |
| 生效 | PUT status=1 | status=0，防重入（DB 已是 1 拒绝） | 校验明细完整（物料/数量/库位），仅锁单，**不触发 ERP** |
| 反生效 | PUT status=0 | status=1 且未执行（入库单未入库/出库单未出库） | 回草稿可编辑；记录操作日志（谁/何时反生效） |
| 执行（入库/出库） | 入库 PUT status=2；出库 PUT /confirm | status=1，防重入（已是 2 拒绝） | 原子事务：库存 + 流水 + ERP（见第 5 节）；入库单整体迁入现有 status=1 分支逻辑 |
| 作废 | 入库 PUT status=9；出库 PUT /void | status=0/1 且未执行 | **逻辑作废**（不物理删明细，保留单据与明细可查）；有 externalCode 则 ERP 弃审+删除；作废后批次/库存释放规则不变 |
| 详情/打印 | GET | 任意 | 不变 |

### 3.5 出库单双状态维度处理

- 保留 `outStoreStatus` 字段（配货计划等联动依赖），但**业务状态以 status 为准**。
- 出库确认时：status 1→2，同时 outStoreStatus 0→1、outStoreTime 落库（现状 status 不变，改造后置 2）。
- 已出库（status=2）后**关闭现状"作废回滚"入口**，统一走退库单（见 4.3 差异说明）。

## 4. 退库单设计（新增）

### 4.1 单据结构（建议）

主表 `warehouse_return_order`：

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint PK | |
| return_no | varchar | 单号（赋码规则 WAREHOUSE_RETURN，留空自动生成） |
| ref_type | tinyint | 来源类型：1=入库退库（原入库单） / 2=出库退库（原出库单） |
| ref_order_id | bigint | 原单据 id |
| ref_order_no | varchar | 原单据号（展示/关联） |
| status | varchar | 0草稿/1已生效/2已完成/9已作废 |
| operator | varchar | 操作人 |
| return_time | datetime | 执行完成时间 |
| total_quantity | decimal | 退库数量合计 |
| remark | varchar | 备注 |

明细表 `warehouse_return_detail`：

| 字段 | 说明 |
|---|---|
| id / return_order_id | 主键 / 关联退库单 |
| ref_detail_id | 原单明细 id（入库明细 / 出库明细） |
| material_id / batch_no | 物料 / 批次 |
| warehouse_location_id | 库位（须与原明细一致） |
| original_quantity | 原单数量 |
| returned_quantity | 原明细已退数量（回写用） |
| return_quantity | 本次退库数量 |
| return_reason | 退库原因（字典 return_reason：错入多入/品质不良/生产退料/客户退货/其他） |

### 4.2 状态机与操作

与主单据完全对称：草稿 → 生效 → 完成；作废旁路。**无审批**。

| 操作 | 前置状态 | 要点 |
|---|---|---|
| 保存/编辑 | 0 | 明细非空；可退数量校验（≤ 原单已入库/出库 − 已退） |
| 生效 | 0 | 锁定；校验明细完整、批次/库位与原明细一致 |
| 反生效 | 1 未执行 | 回草稿 |
| 执行退库 | 1 | 原子事务（见 4.3） |
| 作废 | 0/1 未执行 | 逻辑作废 |

### 4.3 执行退库逻辑（原子事务）

**入库退库（ref_type=1）**：冲销入库
1. 逐行校验：原入库单已完成、库位/批次一致、`return_quantity ≤ original_quantity − returned_quantity`、**当前库存（该批次+库位）≥ return_quantity**；
2. 扣减库存：`warehouse_inventory_info.quantity` 减少（行锁 `getByIdForUpdate`）；
3. 推送 ERP：**红字采购入库**（复用 PurchaseReceiveOpenApi/Create，数量传负数 + 业务类型"采购退货"，见第 5 节）；失败抛异常整体回滚；
4. 回写原入库明细 `returned_quantity += return_quantity`，标记"部分退库/已全部退库"；
5. 写退库完成状态与时间。

**出库退库（ref_type=2）**：冲销出库（货物退回仓库）
1. 逐行校验：原出库单已完成、`return_quantity ≤ 原出库数量 − 已退`；
2. **增加库存**：按批次+物料+库位累加 `warehouse_inventory_info.quantity`（原行存在则累加，不存在则新增）；
3. 推送 ERP：**红字销售出库**（复用 SaleDispatch 通道，数量负数，类型"销售退货"，需验证）；失败整体回滚；
4. 回写原出库明细 `returned_quantity`，标记退库状态；
5. 写退库完成状态与时间。

### 4.4 与现有"生产退料出库/采购退货出库"类型的关系

- 现状出库类型 2/3 是**无原单关联的手工退库**入口，保留不动；
- 新退库单是**关联原单的反向冲销**（自动校验可退量、自动回写原单、自动红字 ERP），两者并存、语义不冲突；
- 原入库单/出库单详情页提供"生成退库单"入口。

## 5. ERP 联动

| 动作 | 通道 | 参数要点 | 状态 |
|---|---|---|---|
| 入库执行 | PurchaseReceiveOpenApi/Create + Audit | 现有逻辑不变 | 现状保留 |
| 出库执行 | SaleDispatch 创建 + Audit | 现有逻辑不变 | 现状保留 |
| 入库退库 | PurchaseReceiveOpenApi/Create（**负数数量**） | 业务类型"采购退货"；erp_api_config 新增退库功能配置（busiTypeCode） | **新增，开发期验证** |
| 出库退库 | SaleDispatch 创建（**负数数量**） | 类型"销售退货" | **新增，开发期验证** |
| 作废（未执行单） | UnAudit + Delete | 现有逻辑不变 | 现状保留 |

> 验证点：① erp_api_config 退库功能开关与 busiTypeCode；② 负数数量红字是否被 T+ 接受（开放平台在线测试）；③ 红字关联原单字段（externalCode 传原单号 or 退库单号）。

## 6. 权限设计（若依按钮级角色权限）

现状：两单 Controller 的 @PreAuthorize 全部被注释 → 需恢复并配置。

### 6.1 权限标识清单

| 单据 | 权限标识（sys_menu 按钮） | 说明 |
|---|---|---|
| 入库单 | warehouse:inboundOrder:query / add / edit / remove | 查询/新增/编辑/删除 |
| | warehouse:inboundOrder:audit | **生效** |
| | warehouse:inboundOrder:unAudit | **反生效** |
| | warehouse:inboundOrder:store | 入库执行 |
| | warehouse:inboundOrder:void / print | 作废 / 打印 |
| 出库单 | warehouse:outStoreOrder:query / add / edit / remove / audit / unAudit / confirm / void / print | 同上（confirm=出库执行） |
| 退库单 | warehouse:returnOrder:query / add / edit / remove / audit / unAudit / confirm / void / print | confirm=执行退库 |

### 6.2 实现方式（不写死）

- 后端：每个操作接口加 `@PreAuthorize("@ss.hasPermi('warehouse:xxx:action')")`；
- 前端：操作按钮加 `v-hasPermi="['warehouse:xxx:action']"`；
- 菜单管理：出入库管理菜单下挂按钮节点，勾选权限标识；角色管理给角色分配菜单即获得按钮权限；
- 生效/反生效等敏感操作由角色配置决定谁可见可点，**代码不写死角色**。

### 6.3 角色配置示例（业务侧在菜单管理配置）

| 角色 | 生效 | 反生效 | 入库/出库执行 | 退库 | 作废 |
|---|---|---|---|---|---|
| 制单员 | ❌ | ❌ | ❌ | ❌ | ✅ |
| 仓管员 | ✅ | ✅ | ✅ | ❌ | ✅ |
| 主管 | ✅ | ✅ | ✅ | ✅ | ✅ |

## 7. 字典 / SQL 变更

```sql
-- 1) 入库单状态字典：重定义
-- wh_inbound_order_status：0未入库→0草稿（保留）、新增 1已生效、2已入库→2已完成、新增 9已作废
INSERT INTO sys_dict_data(dict_type,dict_label,dict_value,dict_sort) VALUES
('wh_inbound_order_status','草稿','0',0),
('wh_inbound_order_status','已生效','1',1),
('wh_inbound_order_status','已完成','2',2),
('wh_inbound_order_status','已作废','9',9);

-- 2) 出库单状态字典：2作废→9作废、新增 2已完成
-- wh_outbound_order_status：0草稿 / 1生效 / 2已完成 / 9已作废

-- 3) 退库单状态字典（新建）
INSERT INTO sys_dict_type(dict_name,dict_type) VALUES ('退库单状态','wh_return_order_status');
INSERT INTO sys_dict_data(dict_type,dict_label,dict_value,dict_sort) VALUES
('wh_return_order_status','草稿','0',0),
('wh_return_order_status','已生效','1',1),
('wh_return_order_status','已完成','2',2),
('wh_return_order_status','已作废','9',9);

-- 4) 退库原因字典（新建）
-- wh_return_reason：错入多入/品质不良/生产退料/客户退货/其他

-- 5) 退库单表（见 4.1，DDL 按项目规范生成）

-- 6) 原单明细加已退数量字段
ALTER TABLE warehouse_rel_inbound_detail ADD COLUMN returned_quantity decimal(18,4) DEFAULT 0 COMMENT '已退数量';
ALTER TABLE warehouse_out_store_order_item ADD COLUMN returned_quantity decimal(18,4) DEFAULT 0 COMMENT '已退数量';
```

> 注意：入库单现状 status=1 数据为"已入库"，改造后 1=已生效、2=已完成 → 需**存量数据迁移**：`UPDATE warehouse_inbound_order SET status='2' WHERE status='1'`；出库单现状 status=2=作废 → `UPDATE warehouse_out_store_order SET status='9' WHERE status='2'`。

## 8. 后端改动点

| 文件 | 改动 |
|---|---|
| WarehouseInboundOrderServiceImpl | updateById 拆分三支：status=1 生效（锁单+校验）、status=2 入库（迁入现 status=1 的过账+ERP 逻辑，事务不变）、status=9 作废（逻辑作废，**去掉物理删明细**）；新增反生效分支（1→0）；**全部操作补状态校验** |
| WarehouseInboundOrderController | 恢复 @PreAuthorize；按第 6 节加权限标识 |
| WarehouseOutStoreOrderServiceImpl | confirm 时 status 1→2；新增反生效（1→0，仅未出库）；void 仅 status=0/1 且未执行（**关闭已出库作废回滚入口**，已出库走退库单）；补状态校验 |
| WarehouseOutStoreOrderController | 恢复 @PreAuthorize；新增 unAudit 接口 |
| WarehouseReturnOrder（新） | Controller + Service + Mapper + DTO/Query/VO + 表结构；执行退库原子事务（4.3）；红字 ERP 推送 |
| IErpPushService / ErpPushServiceImpl | 新增退库红字推送方法（负数数量参数化，复用现有 Create 通道） |
| erp_api_config | 新增退库功能配置（return 功能开关 + busiTypeCode） |

## 9. 前端改动点

| 文件 | 改动 |
|---|---|
| inboundOrder/index.vue + setOption.js | 行操作按新状态机配置 hideWhen：编辑/删除仅 0，生效仅 0，反生效仅 1，入库仅 1，作废仅 0/1，详情/打印全部；去掉作废时的 delInboundOrder 调用 |
| inboundOrder/components/operation.vue | 增加"生效/反生效"按钮与逻辑；入库按钮 status=1 显示 |
| outboundOrder/index.vue + setOption.js | 同上；作废 hideWhen 增加未执行条件 |
| outboundOrder/components/operation.vue | 增加"反生效"按钮 |
| 退库单页面（新） | 参照入库单页面：列表 + 新增/编辑/详情（选择原单→带出明细→填退库数量/原因）+ 生效/反生效/执行退库/作废 |
| 入库/出库单详情 | 增加"生成退库单"入口 + 明细"已退数量"展示 |

## 10. 风险与注意点

1. **存量数据迁移**必须先行（第 7 节 SQL），迁移前备份。
2. 入库单"作废物理删明细"改为逻辑作废后，`checkBatchNoExists` 已排除 status=9 的单，逻辑无需改。
3. 出库单现状"已出库作废回滚"代码保留但入口关闭；历史依赖该能力的需求改为退库单。
4. 红字 ERP 推送为开发期验证项（负数数量是否被 T+ 接受），失败回滚保证一致性，不会产生脏数据。
5. 出库确认接口 confirm 保持独立（不并入 updateById），入库执行并入 updateById 现状不改接口路径（或建议拆独立 confirm 接口，二选一：**建议入库也拆独立接口** `/warehouse/inboundOrder/store/{id}`，与出库对称，职责清晰）。
6. 反生效、生效、执行、作废等敏感操作均需记操作日志（@Log + 操作流水），保证审计。

## 11. 开发顺序建议

1. 字典/SQL/存量迁移 → 2. 后端状态机改造（入库/出库）→ 3. 权限标识与 @PreAuthorize 恢复 → 4. 前端按钮状态机调整 → 5. 退库单（表→后端→前端）→ 6. 红字 ERP 验证与实现 → 7. 联调与存量数据核对。
